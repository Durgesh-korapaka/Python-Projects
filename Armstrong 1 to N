n=int(input())
for i in range(1,n+1):
    temp=i 
    power=0
    orginal= i 
    while temp > 0:
        power += 1 
        temp //= 10
    temp = i 
    sum_of_powers= 0 
    while temp > 0:
        digit = temp % 10
        sum_of_powers += digit ** power
        temp //= 10
    if sum_of_powers == orginal:
        print(orginal)
  
